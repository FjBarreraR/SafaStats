{% extends 'base.html.twig' %}

{% block title %}Error del Sistema{% endblock %}

{% block navbar %}{% endblock %}

{% block body %}
    <div class="container d-flex flex-column justify-content-center align-items-center vh-100">

        <h1 class="mb-4 text-dark fw-bold">¡Impacto de Meteorito!</h1>
        <p class="text-muted mb-5 text-center" style="max-width: 600px;">
            Nuestros sistemas han fallado. Los ingenieros ya están trabajando para restaurar la energía.
            {% if status_code is defined %}
                <br><span class="badge bg-secondary mt-2">Código de error: {{ status_code }}</span>
            {% endif %}
        </p>

        <div class="row justify-content-center w-100">
            <div class="col-10 col-md-5">
                <a href="/" class="text-decoration-none text-white">
                    <div class="card text-center text-white bg-blue card-hover h-100 shadow">
                        <div class="card-body py-5">
                            <img src="{{ asset('images/stats.png') }}" alt="System Error" width="64" height="64" class="icon-white mb-3">
                            <h3 class="card-title">Reiniciar Sistema</h3>
                            <p class="card-text small mt-2">Intentar volver al inicio</p>
                        </div>
                    </div>
                </a>
            </div>
        </div>
    </div>
{% endblock %}
