{% extends 'base.html.twig' %}

{% block title %}
    Dinosaurs
{% endblock %}

{% block body %}

    <h1>Dinosaurs</h1>

    {% for dino in dinosaurios %}
        <p>{{ dino.name }}</p>
        <img src="{{ dino.image }}" style="width: 50%; height: 50%">
    {% endfor %}

{% endblock %}
